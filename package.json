{
  "name": "cinemania-vanilla-ts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npx tsc -w",
    "build:prod": "npx esbuild ./src/index.ts --bundle --minify --outdir=build",
    "build:dev": "npx esbuild ./src/index.ts --bundle --outdir=build --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "dependencies": {
    "express": "^4.21.1"
  }
}
